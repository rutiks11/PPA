#include <stdio.h>
#include "PPA.h"

int main()
{
    struct Demo obj;

    printf("Inside main\n");
    printf("Rate of interest is %f\n", ROI);
    printf("Fees of logic Building BAtch %d\n", FEES);
    printf("Rate of interest is %d\n", sizeof(obj));
    
    return 0;
}